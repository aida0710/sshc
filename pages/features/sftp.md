---
title: SFTP
description: リモートファイルの操作と、中断・再開に対応したSFTP転送マネージャー。
---

# SFTP

![SFTPのファイルブラウザー](/images/sftp-desktop.png)

SFTP画面では、接続先を選ぶまで通信を開始しません。接続すると、SFTPサーバーが返すログインユーザーのホームディレクトリを最初に開きます。接続には、既存のSSH設定、ホスト鍵、保存済みの認証情報をそのまま使っています。

接続先を切り替えると、前の接続先の一覧と開いていたファイルをすぐに閉じます。切り替え前の通信が遅れて完了しても、その内容を新しい接続先の画面には表示しません。

## ファイル操作

- リモートディレクトリの移動、作成、名前変更、`chmod`、削除
- ファイルやフォルダーを、選択またはドラッグ＆ドロップでアップロード
- ファイルのダウンロード、フォルダーのZIPダウンロード
- 2 MiB以下のUTF-8テキストをMonaco Editorで編集

一覧の先頭にある`..`で親ディレクトリへ移動します。上部の移動ボタンでは、訪れたディレクトリを戻る／進む、ホームディレクトリへ戻る、ルートディレクトリを開く操作ができます。一覧は名前で絞り込めます。行を1回選ぶか、チェックボックスで複数の項目を選択して、右上の操作メニューからダウンロードや削除を実行できます。デスクトップではShift+クリックで範囲選択、Ctrl/Cmd+クリックで追加選択、Ctrl/Cmd+Aですべて選択できます。操作メニューでは選択を反転し、名前またはフルパスをまとめてコピーできます。権限変更と名前変更は1件だけ選んだときに利用できます。ダブルクリックまたはEnterでフォルダーを開くか、一覧の幅を変えないモーダルでテキストファイルを編集できます。作成とアップロードは左上の`+`メニューにまとめています。

一覧は、名前、種類、サイズ、更新日時で並べ替えられます。権限は名前の下に表示します。選択中の接続先とディレクトリはURLにも反映されるため、Terminalで見つけたリモートパスをSFTPでそのまま開けます。

## 転送マネージャー

ファイル一覧の下部にある転送マネージャーで、ファイルとフォルダーを1つのキューとして管理でき、既定では2件まで同時に転送できます。キューは見出しだけに折りたため、操作メニューからすべての転送をまとめて一時停止、再開、キャンセルできます。ファイルごとの進捗、速度、残り時間を確認しながら、1件ずつ操作したり、失敗したファイルだけをやり直したりすることも可能です。

大きなファイルのアップロードには、リモート側の一時ファイルを使っています。完了後に正式なファイル名へ置き換える仕組みのため、接続が切れても転送済みの位置から再開できます。ファイルのダウンロードはHTTP Rangeに対応し、保存済みのローカルデータとリモートのリビジョンが一致すれば途中から再開します。

キューと状態はエンジン側の転送マネージャーが管理し、端末上のファイルを読み書きする処理はブラウザー／WebViewが担当します。同じアプリ内でSFTP画面から移動しても転送は続きますが、ブラウザーやWebViewを閉じるとデータの送受信は停止します。再読み込み後のアップロードには同じローカルファイルの再選択が必要です。フォルダーのZIPダウンロードは途中から再開せず、先頭からやり直します。

操作方法や再開できる条件は、[転送マネージャー](/sftp/transfers)を参照してください。
