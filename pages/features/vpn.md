---
title: 接続ごとのVPN
description: 選んだSSH接続だけを専用のVPN経由でつなぐ。ホストのルーティングもDNSも変えない。
---

# 接続ごとのVPN

選んだSSH接続だけを、その接続専用のVPN経由でつなぎます。ホストのルーティング、DNS、VPNクライアントの設定は変わりません。**ホストを別のVPNにつないだまま、そのVPNの外にあるホストへ接続できます。**

VPNプロファイルには、VPNへ接続するための設定とシークレットだけを保存します。接続先は持ちません。パスワードと同じように、Connectionsで接続にVPNプロファイルを付けると、その接続の`HostName`と`Port`へVPN経由で接続します。1つのプロファイルを複数の接続に付けられます。

この機能はDockerが動いているマシンで使用できます。Dockerが無い場合や起動していない場合は、その理由を表示して処理を中止します。

## 仕組み

```text
sshc engine
   │ 接続1本ごとに docker exec（標準入出力）
   ▼
VPN専用コンテナの中継
   │ 接続先へのTCP接続
   ▼
トンネル ─ VPN ─ Dockerのネットワーク ─ VPNサーバー
```

SSHのハンドシェイク、認証、ホスト鍵の照合、ssh-agentの利用は、engineがホスト側で行います。コンテナへ渡すのは接続先のアドレスとポートだけで、SSH秘密鍵もVaultの鍵も渡しません。

engineは、接続を1本開くたびに`docker exec`でコンテナの中継を起動し、その標準入出力を接続として使います。ホストとコンテナのあいだでソケットを共有しないため、LinuxでもmacOSやWindowsのDocker Desktopでも同じ仕組みで動きます。

コンテナは、接続に使われた接続先への経路とパケットフィルタだけを作ります。VPNの先のネットワーク全体は取り込みません。接続先への通信はfail closedです。トンネル以外から接続先へ出ようとする通信は、コンテナ内で拒否します。VPNが切断されているあいだ、接続先への通信がDockerの通常のネットワークへ流れることはありません。

## ホストへの影響

ホストのルーティング、DNS、NetworkManager、接続中のVPNの設定は変更しません。`--privileged`と`--network host`も使いません。コンテナへ渡すデバイスは方式ごとのトンネル用デバイス（WireGuardとOpenConnectは`/dev/net/tun`、L2TP/IPsecは`/dev/ppp`）だけです。権限も方式ごとに必要なものだけを渡し、WireGuardでは`CAP_NET_ADMIN`・`CAP_NET_RAW`・`CAP_DAC_OVERRIDE`・`CAP_CHOWN`以外をすべて外します。

ただし、ホストとまったく無関係ではありません。Dockerのbridgeとカーネルのトンネル機能を使います。また、Dockerを操作できるユーザーは、一般に強いホスト権限を持ちます。分離されるのは、VPNのルーティング・DNS・接続状態と、VPNへ送るアプリケーションの通信です。

## プロファイルを作成する

VPN画面、または`sshc vpn add <名前>`で作成します。

| 項目 | 内容 |
|---|---|
| 方式 | WireGuard、L2TP/IPsec、OpenConnect（AnyConnect、ocserv、GlobalProtectなど） |
| VPN内のDNSサーバー | `HostName`をホスト名で書いた接続に使う場合だけ入力します。IPv4アドレスを3件まで |
| VPNサーバー | WireGuardは`host:port`、L2TP/IPsecとOpenConnectはホスト名またはIPアドレス |
| シークレット | WireGuardは秘密鍵、L2TP/IPsecはVPNのパスワードとIPsecの事前共有鍵、OpenConnectはVPNのパスワード |

シークレットはVaultに保存します。保存後は、画面にもAPIの応答にも表示されません。

作成と編集は別の操作です。同じ名前のプロファイルがすでにある場合、作成は何も変更せずにエラーになります。設定を変える場合は、VPN画面の「編集」または`sshc vpn edit <名前>`を使います。名前だけを変える場合は、名前の変更を使ってください。Vaultに同じ名前のシークレットが残っていても、作成時に入力したシークレットで置き換え、古いシークレットは引き継ぎません。

編集では、空欄のままにしたシークレットは保存済みの値がそのまま残ります。設定だけを直す場合に、シークレットを入力し直す必要はありません。方式を変えた場合は、前の方式のシークレットを削除し、新しい方式のシークレットの入力を求めます。`sshc vpn edit`では、各項目の初期値が保存済みの値になり、任意の項目は`-`を入力すると消せます。設定とシークレットは1回の書き込みで保存するため、どちらか一方だけが書き換わることはありません。

使用できない値がある場合は、どの項目のどこが問題か（未入力、形式の誤り、件数の上限など）を画面と`sshc vpn add`に表示します。

L2TP/IPsecでは、古いVPN機器と暗号スイートが合わない場合だけ、IKEとESPの暗号スイートを指定します。空欄なら既定値を使います。

OpenConnectでは、サーバーのプロトコル（`anyconnect`、`nc`、`pulse`、`gp`、`f5`、`fortinet`、`array`）を選びます。分からない場合は`anyconnect`のままにしてください。自己署名証明書を使うサーバーでは、証明書のフィンガープリントを指定します。`sha256:`（証明書のSHA-256、16進）と`pin-sha256:`（公開鍵のPIN、base64）のどちらでも指定できます。指定しない場合は通常どおり証明書を検証し、検証できなければ接続しません。OpenConnectでは、サーバーが配布するデフォルトルートとDNSは設定しません。ルーティングは、接続に使った接続先の分だけを作ります。

### 二要素認証（Duo Mobileなど）

パスワードのあとにサーバーが追加の認証を求める場合は、「二要素認証」を指定します。

| 選択 | 送る値 | 使う場面 |
|---|---|---|
| なし | 何も送らない | パスワードだけで認証するサーバー |
| スマートフォンで承認する（Duo Mobileなど） | 何も送らない（文字列を入力した場合はその文字列） | Duo Mobileでの承認 |
| 保存したシークレットからワンタイムパスワードを生成する | その場で生成した6桁のコード | TOTPのシークレットを登録できるサーバー |

Duoのサーバーには2種類の動き方があります。**パスワードを受け取った時点でスマートフォンへ通知を送り、承認されるまで応答を返さないもの**と、**`push`のような文字列の入力を追加で求めるもの**です。前者では「二要素認証で送る文字列」を空欄にします。後者の場合だけ、その文字列を入力します。

どちらの場合も、待ち時間の上限は2分になります。通知に気付いてスマートフォンを開き、承認するまでの時間を見込んでいます。待っているあいだ、VPN画面と`sshc vpn`の一覧には「スマートフォンでの承認を待っています」と表示されます。

TOTPのシークレットはVaultに保存し、コードはコンテナへ渡す直前に生成します。シークレットも生成したコードも、`docker logs`と画面には表示されません。

Duoの「Universal Prompt」のように、ブラウザでのSAMLログインを求める構成には対応していません。コンテナの中にはブラウザが無く、engineがその操作を代わりに行うこともありません。

### 接続先をVPN内のホスト名で指定する

接続の`HostName`をホスト名で書いた場合は、そのホスト名の名前解決に使うDNSサーバーを、付けるVPNプロファイルに指定します。名前解決はコンテナの中で、指定したDNSサーバーだけを使って行います。ホストのresolv.confも、コンテナ既定のDNSも使いません。同じ名前がホスト側では別のマシンを指す環境で、接続先を取り違えないためです。DNSサーバーを指定していないプロファイルでは、ホスト名の接続先はエラーになります。Connectionsでプロファイルを選んだときにも注記が表示されます。

DNSサーバーへの通信もトンネルの中だけに流します。名前解決で得たアドレスは、コンテナのログ（VPN画面の「ログ」、`sshc vpn logs`）に記録されます。

名前解決は、経路を起動してから初めてその接続先へ接続するときに1回行います。その後に名前が別のアドレスを指すようになった場合は、経路を切断してから接続し直してください。

## 接続に付ける

パスワードと同じように、VPNプロファイルは接続の側で付けます。次のどちらかで付けます。どちらも同じ設定になります。

- ［Connections］の［sshc］タブで「使用するVPNプロファイル」を選ぶ
- `sshc vpn bind <alias> <プロファイル名>`を実行する（外す場合は`sshc vpn unbind <alias>`）

VPN画面では、そのプロファイルを使っている接続の一覧を確認できます。

この設定は`~/.ssh/config`には書きません。OpenSSHの設定項目ではなく、sshcが管理する設定だからです。

付けた接続は、Terminal、SFTP、`sshc <接続先>`のどれから開いても同じ経路を使います。接続先はその接続の`HostName`と`Port`です。経路を用意できないときは、通常のネットワークへ切り替えずに接続を中止します。

どの接続がどの経路を使うかは、接続の前にも後にも確認できます。TerminalとSFTPの見出しには「VPN経由: <名前>」が表示されます。`sshc info <alias>`の`vpn`行にも同じ名前が表示されます。

プロファイルを削除すると、そのシークレットと、そのプロファイルを付けていた接続の設定も同時に削除されます。接続中の経路は切断します。削除にはVaultのロック解除が必要で、ロック中は何も変更せずにエラーになります。設定だけを削除してシークレットが残ると、同じ名前で作り直したプロファイルに古いシークレットが混ざるためです。

名前を変える場合は、削除して作り直さずに、VPN画面の「名前を変更」または`sshc vpn rename <古い名前> <新しい名前>`を使います。設定、シークレット、接続に付けた設定がまとめて新しい名前へ移ります。名前の変更にもVaultのロック解除が必要です。新しい名前の形式、同じ名前のプロファイルの有無、Vaultの状態を確認してから経路を切断するため、名前の変更がエラーになった場合、接続中の経路は切断されません。

## 接続が完了するまで

`sshc vpn up`とVPN画面の「接続」は、経路が使用できるようになるまで待ちます。コンテナイメージをまだ作成していないマシンでは、最初の1回だけ数分かかります。進み具合は「コンテナイメージを準備しています」「コンテナを起動しています」「VPNの接続を待っています」の順に表示されます。

## 経路の起動と停止

経路は、必要になったときに起動し、不要になったら停止します。接続先への経路とパケットフィルタは、その接続先へ初めて接続するときに追加します。engineを終了すると、起動していた経路もすべて停止します。通っている接続が1本も無い状態が10分続いた経路も停止します。Terminal、SFTP、`sshc <接続先>`、`sshc vpn proxy`のどれから開いた接続も同じように数えるため、使用中の経路が停止することはありません。`sshc vpn up`や画面の「接続」で起動しただけの経路は、起動した時刻から数えます。VPNが切断された経路は、中継だけを残さず、コンテナごと終了します。

接続を待っているあいだに`Ctrl-C`で中断した場合や、画面を閉じた場合も、準備中だったコンテナは残りません。

経路を停止するときは、VPNサーバーへ切断を通知してから終了します。OpenConnectではサーバーへログアウトを送り、L2TP/IPsecではL2TPの切断とIPsecの終了を送ります。サーバー側にセッションが残らないため、同時接続数の枠も解放されます。

停止したあとに同じ接続を開くと、経路は自動で再起動します。スマートフォンでの承認が必要な経路では、そのときにもう一度承認を求められます。

## 接続できないとき

経路が起動していれば、VPN画面と`sshc vpn`の一覧に、トンネルのインターフェース名、トンネルのアドレス、接続開始時刻が表示されます。ここまで表示されていれば、VPNの接続は確立しています。WireGuardでは、サーバーとのハンドシェイクが完了するまで経路を開きません。鍵やサーバーの指定が間違っている場合は、経路を開かずに原因を表示します。

VPNの接続に失敗した場合は、分かる範囲で原因（ハンドシェイクに失敗した、PPPの認証に失敗したなど）を画面と`sshc vpn up`に表示します。

VPNには接続できたが接続先へ届かない場合は、原因をTerminalと`sshc <接続先>`に表示します。

| 表示 | 確認すること |
|---|---|
| VPN内のDNSサーバーで接続先の名前解決に失敗しました | プロファイルのDNSサーバーと、接続の`HostName` |
| 接続先が応答しないか、接続を拒否しました | 接続の`HostName`と`Port`、接続先でSSHサーバーが動いているか |
| 接続先がVPNサーバーと同じアドレスです | VPNサーバーそのものにはVPN経由で接続できません |

VPNのシークレットが無い、Dockerが起動していないなど、設定を直さない限り同じ理由で失敗する場合、Terminalは再接続を繰り返しません。

表示されない場合や、VPNには接続できているのに接続先へ到達できない場合は、VPN画面の「ログ」、または`sshc vpn logs <名前>`でログを確認します。ログには、「sshcエンジンの記録」と「コンテナのログ」を順に表示します。sshcエンジンの記録は、engineがVPN経路を用意した手順（使ったdocker、コンテナイメージの作成、実行したdockerのコマンドと失敗したときの出力）です。コンテナイメージの作成に失敗した場合のように、コンテナが起動する前に失敗した場合も、ここで原因を確認できます。接続に失敗したコンテナは削除しますが、そのときのログはengineが保持しているので、あとから同じ方法で確認できます。保存済みのシークレットは`[REDACTED]`に置き換えて表示するため、そのまま貼り付けて共有できます。`docker logs`を直接実行する必要はありません。

## ホストの`ssh`・`scp`・`git`から使う

`ProxyCommand`に`sshc vpn proxy`を指定すると、sshc以外のSSHクライアントも同じ経路を使えます。接続先は`%h %p`で渡します。省略できません。

```text
Host lab
  HostName 10.9.9.1
  ProxyCommand sshc vpn proxy tohoku %h %p
```

接続先へ届かない場合は、通常のネットワークへ切り替えずに接続を中止し、原因を標準エラーに表示します。SSHのハンドシェイク・鍵・known_hostsは呼び出した側のものを使い、sshcはデータを中継するだけです。

## 制約

- 接続先はIPv4アドレスかホスト名です。IPv6アドレスには対応していません。VPNサーバーそのものは接続先にできません。
- 踏み台（ProxyJump）の先のホップには使えません。2ホップ目以降は手前のSSH接続の中を通るため、このマシンのVPNを経由できません。
- VPNプロファイルを付けた接続では`ProxyCommand`を使えません。`ProxyCommand`はこのマシンで実行されるため、VPNの中を通りません。`ProxyCommand`から経路を使う場合は、プロファイルを付けずに上の`sshc vpn proxy`を指定します。
- 現在はLinuxでだけ動作を確認しています。
