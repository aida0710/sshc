---
title: 接続ごとのVPN
description: 選んだSSH接続だけを専用のVPNへ通す。ホストの既定経路もDNSも変えない。
---

# 接続ごとのVPN

選んだSSH接続だけを、その接続専用のVPNへ通します。ホストの既定経路、DNS、VPNクライアントの設定は変わりません。**ホストが別のVPNに繋がったまま、そのVPNの外にしかいないホストへ接続できます。**

この機能はDockerが動いている機械で使えます。Dockerが無い場合は、その理由を表示して拒否します。

## 何が起きているか

```text
sshc engine
   │ Unixソケット（利用者だけが開ける）
   ▼
VPN専用コンテナの中継
   │ 接続先へのTCP接続
   ▼
トンネル ─ VPN ─ Dockerの通常回線 ─ VPNサーバー
```

SSHの握手、認証、ホスト鍵の照合、ssh-agentの利用はengineがホスト側で行います。コンテナへ渡すのは「どこへTCPを張るか」だけで、SSH秘密鍵もVaultの鍵も渡しません。

接続先への通信はfail closedです。トンネル以外から接続先へ出ようとする通信はコンテナ内で拒否します。トンネルが落ちているあいだ、接続先への通信がDockerの通常回線へ流れることはありません。

## ホストに影響しない範囲

ホストの既定経路、DNS、NetworkManager、既に繋いでいるVPNの設定は変更しません。`--privileged`と`--network host`も使いません。コンテナへ渡す追加権限は`CAP_NET_ADMIN`、渡すデバイスは方式ごとのトンネル用デバイス（WireGuardは`/dev/net/tun`、L2TP/IPsecは`/dev/ppp`）だけです。

完全に無関係ではありません。Dockerのbridgeとカーネルのトンネル機能を使います。Dockerを操作できる利用者は一般に強いホスト権限を持ちます。隔離するのは、VPNの経路・DNS・接続状態と、VPNへ送るアプリケーション通信です。

## プロファイルを作る

VPN画面（または`sshc vpn add <名前>`）から作成します。1つのプロファイルは1つの接続先へ届きます。VPNの向こうのネットワーク全体を引き込まないので、経路とパケットフィルタが接続先ひとつで閉じます。

| 項目 | 内容 |
|---|---|
| 方式 | WireGuard、またはL2TP/IPsec |
| VPNの中の接続先 | `host:port`。VPN内のDNSは使わないため、IPv4アドレスを指定します |
| VPNサーバー | WireGuardは`host:port`、L2TP/IPsecはホスト名またはアドレス |
| 秘密 | WireGuardは秘密鍵、L2TP/IPsecはVPNのパスワードとIPsecの事前共有鍵 |

秘密はVaultに保存します。画面にもAPIの応答にも戻りません。保存済みの秘密を残したまま設定だけを直せます。

L2TP/IPsecで古い装置と暗号方式が合わない場合だけ、IKEとESPの候補を指定します。空なら既定に任せます。

## 接続へ結び付ける

VPN画面で接続を選んで「この経路を通す」を押すか、`sshc vpn bind <alias> <プロファイル名>`を実行します。紐付けは`~/.ssh/config`には書きません。OpenSSHが解釈する語ではなく、sshcが持つ設定だからです。

結び付けた接続は、Terminal、SFTP、`sshc <接続先>`のいずれからでも同じ経路を通ります。経路が無いときは、素の回線へ落とさずに拒否します。

プロファイルを削除すると、その秘密と、そのプロファイルを指していた接続の紐付けも同時に消えます。名前を変えるときは削除して作り直さず、VPN画面の「名前を変える」または`sshc vpn rename <古い名前> <新しい名前>`を使います。設定・秘密・紐付けがまとめて新しい名前へ移ります。

## 繋がらないとき

経路が開いていれば、VPN画面と`sshc vpn`の一覧に、トンネルのインターフェース名、トンネル側のアドレス、開始時刻が出ます。ここまで出ていれば、トンネルは成立しています。

出ていない場合、またはトンネルはできているのに接続先へ届かない場合は、コンテナの出力を読みます。VPN画面の「ログ」または`sshc vpn logs <名前>`です。保存済みの秘密は`[REDACTED]`に置き換えて表示するので、そのまま貼って共有できます。`docker logs`を直接叩く必要はありません。

## 制約

- 1プロファイルにつき接続先は1つ、IPv4です。
- 踏み台（ProxyJump）の向こうのホップには使えません。2ホップ目以降は手前のSSH接続の中を通るため、この機械のVPNを通る余地がありません。
- `ProxyCommand`との併用はできません。`ProxyCommand`はこの機械で走るので、VPNの中にはいません。
- VPN内のDNSは使いません。接続先はIPv4アドレスで指定します。
- ホストの`ssh`・`scp`・`git`からは使えません。sshcのTerminal、SFTP、CLIから使います。
- 現在はLinuxで動作を確認しています。
