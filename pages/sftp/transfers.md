---
title: 転送マネージャー
description: ファイルやフォルダーの転送をまとめて管理し、一時停止、再開、再試行、キャンセルする。
---

# 転送マネージャー

![日本語表示の転送マネージャー](/images/transfer-manager-ja.png)

ファイルとフォルダーのアップロード／ダウンロードに加え、2つのSFTP接続間で行うRemote→Remoteコピー／移動を、1つのキューで管理できます。既定では2件まで同時に転送できます。転送マネージャーはSFTP画面の下部へドックされ、通常は転送件数、全体の進捗、速度だけを表示します。クリックして展開すると、ファイルごとの状態と操作を確認できます。

## 表示される情報

- 全体とファイルごとの進捗
- 転送済み／合計サイズ
- 現在の速度と残り時間
- 待機中、転送中、一時停止、完了、失敗、キャンセルの状態

## 操作

- 一時停止すると、新しい読み書きを止めて再開情報を保持します
- 再開すると、条件を満たすファイル転送を途中から続行します
- 再実行すると、失敗したファイルだけをもう一度転送します
- キャンセルすると、その転送を中止します
- 完了分を消去すると、完了またはキャンセル済みの項目を一覧から除去します

アップロード中のデータは、同じディレクトリの一時ファイルへ保存されています。完了後に正式なファイル名へ置き換えられ、既存ファイルを上書きする場合は転送前に確認が表示されます。再読み込み後にアップロードを続けるには、同じローカルファイルを選び直してください。ファイルの名前、サイズ、更新日時が一致し、リモート側の一時ファイルも利用できる場合は、転送済みの位置から続行します。

ファイルのダウンロードは、ブラウザー側に保存したデータとリモートのリビジョンが一致する場合にHTTP Rangeで再開します。フォルダーはZIPとして送信されますが、ZIPの途中からは再開できません。一時停止、失敗、再読み込みの後は0バイトからやり直します。Androidでは完成したZIPをシステムのファイル選択画面へ渡します。

## 画面を離れた場合

転送キューと状態はエンジン側の転送マネージャーが管理し、`~/.ssh/sshc/transfers.json`へ原子的に保存します。登録、待機順、進捗、同時実行数、上書き確認、再開用チェックポイントは、ブラウザー／WebViewの再読み込みだけでなく、エンジンの再起動後にも復元されます。画面は2秒ごとにエンジンの状態を読み直すため、複数の画面を開いた場合も同じキューへ揃います。

ローカルファイルの読み書きだけは、端末のファイルへアクセスできるブラウザー／WebViewが実行します。そのため画面を閉じるとデータの送受信は止まりますが、ジョブ自体がブラウザーの保存領域だけに取り残されることはありません。再読み込み後のアップロードでは「再開待ち」と表示され、元のファイルを選び直すまで送信しません。

Remote→Remote転送はエンジンが2つのSFTP接続を開いて直接ストリームするため、ブラウザーを閉じても継続します。ファイルは転送先の一時名へ書き、完了時に正式名へ原子的に置き換えます。エンジンの終了中に転送していたジョブは再起動時に待機状態へ戻り、自動的に再実行されます。
