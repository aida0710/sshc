---
title: 転送マネージャー
description: ファイルやフォルダーの転送をまとめて管理し、一時停止、再開、再試行、キャンセルする。
---

# 転送マネージャー

![ファイル転送中の転送マネージャー](/images/transfer-manager.png)

ファイルとフォルダーのアップロード／ダウンロードを、1つのキューで管理できます。既定では2件まで同時に転送できます。

## 表示される情報

- 全体とファイルごとの進捗
- 転送済み／合計サイズ
- 現在の速度と残り時間
- 待機中、転送中、一時停止、完了、失敗、キャンセルの状態

## 操作

- 一時停止すると、新しい読み書きを止めて再開情報を保持します
- 再開すると、条件を満たすファイル転送を途中から続行します
- 再実行すると、失敗したファイルだけをもう一度転送します
- キャンセルすると、その転送を中止します
- 完了分を消去すると、完了した項目を一覧から除去します

アップロード中のデータは、同じディレクトリの一時ファイルへ保存されています。完了後に正式なファイル名へ置き換えられ、既存ファイルを上書きする場合は転送前に確認が表示されます。再読み込み後にアップロードを続けるには、同じローカルファイルを選び直してください。ファイルの名前、サイズ、更新日時が一致し、リモート側の一時ファイルも利用できる場合は、転送済みの位置から続行します。

ファイルのダウンロードは、ブラウザー側に保存したデータとリモートのリビジョンが一致する場合にHTTP Rangeで再開します。フォルダーはZIPとして送信されますが、ZIPの途中からは再開できません。一時停止、失敗、再読み込みの後は0バイトからやり直します。Androidでは完成したZIPをシステムのファイル選択画面へ渡します。

## 画面を離れた場合

転送キューと状態はエンジン側の転送マネージャーが管理します。登録、待機順、進捗、同時実行数、上書き確認、再開用チェックポイントは、ブラウザー／WebViewを再読み込みしてもエンジン側に残ります。複数の画面を開いた場合も、各画面は同じキューを表示します。

ローカルファイルの読み書きだけは、端末のファイルへアクセスできるブラウザー／WebViewが実行します。そのため画面を閉じるとデータの送受信は止まりますが、ジョブ自体がブラウザーの保存領域だけに取り残されることはありません。再読み込み後のアップロードには元のファイルを選び直してください。エンジンを終了するとメモリー上のキューも終了するため、再起動後は転送を登録し直す必要があります。
