---
title: はじめる
description: sshcを起動し、最初の接続を開くまで。
---

# はじめる

sshcは既存の`~/.ssh/config`を読み、ローカルでWeb UIとAPIを提供します。

```sh
sshc engine
```

別のターミナルからUIを開きます。

```sh
sshc
```

初回は画面の案内に従ってvaultを作成してください。既存の`Host`設定は「接続」に表示されます。
