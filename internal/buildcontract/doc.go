// Package buildcontract は、このリポジトリのビルドとリリースの取り決めを検査する。
//
// **ここには本番のコードが無い。** あるのは Makefile と .github/workflows と
// scripts/ を読み、それらが約束を守っているかを表明する検査だけである——native な
// レシピが固定の argv だけを渡すこと、リリースが全プラットフォームをネイティブに
// 焼くこと、成果物の名前の検証がシェルの展開を通らないこと。
//
// **道具の方はここに居ない。** ネイティブビルドの CLI（`go run` で Makefile から
// 呼ばれる実体）と、焼けた実行ファイルの機種を見る道具は internal/nativebuild に
// ある。両方が同じパッケージに居た間、名前は「契約」と言いながら中身の半分は
// 実装であり、**その実装をテストする検査と、リポジトリを検査する検査が同じ画面に
// 並んでいた。**
package buildcontract
